---
"@posthog/convex": minor
---

Initial release with `capture`, `identify`, `groupIdentify`, and `alias` methods. Feature flag support: `getFeatureFlag`, `isFeatureEnabled`, `getFeatureFlagPayload`, `getFeatureFlagResult`, `getAllFlags`, and `getAllFlagsAndPayloads`.
